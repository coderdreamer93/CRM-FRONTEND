import React from "react";
import { Typography, Paper } from "@mui/material";

const Sales = () => {
  return (
    <Paper sx={{ p: 3 }}>
      <Typography variant="h4">Sales Page</Typography>
      <Typography variant="body1">Here you can manage your sales data.</Typography>
    </Paper>
  );
};

export default Sales;
